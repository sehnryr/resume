[package]
name = "resume"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
minify-html = "0.16.4"
serde = { version = "1.0.219", features = ["derive"] }
toml = { version = "0.8.23", features = ["preserve_order"] }
